package exceptions;

public class ValidationInstruction {
    String length;
    String prefix;

    public ValidationInstruction(String length, String prefix) {
        this.length = length;
        this.prefix = prefix;
    }
}
